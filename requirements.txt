# Runtime deps for ComfyUI-MiniMaxH3-SPEED-Sampler.
# torch is provided by ComfyUI at runtime; pin here only if you install
# standalone. Keep no pinned versions so ComfyUI's own torch is respected.
torch>=2.0
